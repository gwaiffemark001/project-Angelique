# Angelique Trading Engine v2

This package is a drop-in replacement for the existing `trading` package.

It is designed to work with the redesigned `skills/trading_skill` package and keeps the existing bridge/account/market facade names. Replace the existing `trading` directory together with the redesigned `skills/trading_skill` directory.

Test on a demo account first. The package does not guarantee profitable trading.
